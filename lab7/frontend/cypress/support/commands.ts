// cypress/support/commands.ts
// Здесь можно добавлять кастомные команды Cypress, например:
// Cypress.Commands.add('login', (username, password) => { ... });

// Пустой экспорт, чтобы файл был модулем TypeScript и esbuild его корректно обрабатывал.
export {};
