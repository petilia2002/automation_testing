import { ResultColorDirective } from './result-color.directive';

describe('ResultColorDirective', () => {
  it('should create an instance', () => {
    const directive = new ResultColorDirective();
    expect(directive).toBeTruthy();
  });
});
