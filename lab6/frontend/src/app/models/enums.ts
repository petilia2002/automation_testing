export enum OperationType {
  ADD = 'ADD',
  SUBTRACT = 'SUBTRACT',
  MULTIPLY = 'MULTIPLY',
  DIVIDE = 'DIVIDE',
}

export enum NumberSystem {
  BINARY = 'BINARY',
  OCTAL = 'OCTAL',
  DECIMAL = 'DECIMAL',
  HEXADECIMAL = 'HEXADECIMAL',
}
